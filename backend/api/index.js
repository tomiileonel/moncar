// Vercel detecta este archivo por convención de carpeta (api/) y lo despliega
// automáticamente como Serverless Function — sin necesidad de "builds" legacy.
export { default } from '../dist/server.js';
